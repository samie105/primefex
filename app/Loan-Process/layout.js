import "../globals.css";
import "../styles.css";

export const metadata = {
  title: "Primefexloans | How it works",
};

export default function LoanProcessLayout({ children }) {
  return <main>{children}</main>;
}
