import "../globals.css";
import "../styles.css";

export const metadata = {
  title: "Primefexloans | About Us",
};

export default function LoanProcessLayout({ children }) {
  return <main>{children}</main>;
}
