import "../globals.css";
import "../styles.css";

export const metadata = {
  title: "Primefexloans | Loan Application",
};

export default function LoanProcessLayout({ children }) {
  return <main>{children}</main>;
}
